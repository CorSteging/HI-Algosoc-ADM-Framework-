# HI-Algosoc-ADM-Framework

## Running

```powershell
python main.py --k 10
```

This outputs a JSON object with the top-k rankings from `model_1` through `model_5`.

## Notes

- Data source: `data/applicant_cvs.json`
- You can change `k` to any positive integer.
